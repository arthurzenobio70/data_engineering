# Core requirements for running the dbt project
# Install these first, then run 'dbt deps' for dbt packages

# Essential dbt and Snowflake
dbt-core>=1.6.0,<2.0.0
dbt-snowflake>=1.6.0,<2.0.0

# Basic monitoring
streamlit>=1.25.0,<2.0.0
pandas>=1.5.0,<3.0.0
snowflake-connector-python>=3.0.0,<4.0.0

# Basic utilities
python-dotenv>=0.19.0,<2.0.0
pyyaml>=6.0.0,<7.0.0
requests>=2.28.0,<3.0.0

# Note: dbt packages (dbt-utils, dbt-expectations, etc.) 
# are installed via 'dbt deps' command, not pip
